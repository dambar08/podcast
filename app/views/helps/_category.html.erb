<%# locals: (category:) %>
<div id="<%= dom_id category %>" class="bg-black text-white p-8">
  <div class="text-2xl text-center font-bold"><%= category.title %></div>
  <div class="text-center"><%= category.description %></div>
</div>
